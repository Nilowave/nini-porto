{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs-18_x", "yarn", "python3", "gcc", "gnumake"]
    },
    "install": {
      "cmd": "yarn install --ignore-engines"
    },
    "build": {
      "cmd": "yarn build"
    }
  }
}

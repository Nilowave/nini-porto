{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_18", "yarn", "python3", "gcc", "gnumake"]
    },
    "install": {
      "cmd": "yarn install --ignore-engines"
    },
    "build": {
      "cmd": "yarn build"
    }
  }
}

import styled from 'styled-components';

export const StyledIcon = styled.div`
  --size: 2.4rem;
  width: var(--size);
  height: var(--size);

  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0.4rem;
`;

import styled from 'styled-components';

export const StyledS04SocialSharing = styled.div`
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 2rem;
`;

export const StyledLink = styled.a`
  transition: opacity 0.4s ease;

  &:hover {
    opacity: 0.7;
  }
`;

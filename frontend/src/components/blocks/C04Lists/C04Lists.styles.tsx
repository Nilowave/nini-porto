import styled from 'styled-components';
import { Flex } from 'styles/layout';

export const StyledC04Lists = styled.div``;
export const List = styled(Flex)``;
export const ListItem = styled(Flex)``;
export const Check = styled.span``;

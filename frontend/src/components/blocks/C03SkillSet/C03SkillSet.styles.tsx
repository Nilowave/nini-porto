import styled from 'styled-components';
import { BlockComponent } from 'styles/layout';

export const StyledC03SkillSet = styled(BlockComponent)`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  column-gap: 3rem;
`;
